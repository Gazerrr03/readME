# Portfolio OS Design Language

> 本文档是后续新增页面、窗口、组件、图标、动效与内容呈现的设计约束。
> 它总结的是当前已实现的视觉语言，不是对某个真实操作系统的复刻规范。

## 1. 设计定位

### 核心描述

这是一个发生在凌晨两点的、虚构的个人作品集操作系统：冷静、精确、克制，带有 1-bit 数字设备与早期图形界面的物理感，但不依赖怀旧装饰。

界面首先应当像一个**可以操作的系统**，其次才像一个网站。每个元素都应表达状态、层级或功能；装饰只有在强化“信号、网格、协议、显示设备”这一系统语义时才成立。

### 设计关键词

- 二值：蓝 / 白、开 / 关、选中 / 未选中、运行 / 最小化。
- 结构外露：边框、网格、标题栏、分隔线和硬阴影直接说明组件如何组织。
- 系统化：同一状态使用同一视觉反馈，不为单一页面随意创造新语言。
- 稀疏而非空洞：桌面保留大面积工作区，窗口内部保持紧凑、可扫描的信息密度。
- 技术但有人格：系统文案理性简短，Times 风格标题为冷硬界面保留文学气质。
- 复古但不拟古：借用 1-bit 图形、终端协议与桌面隐喻，不复刻特定年代或品牌的 UI。

## 2. 约束层级

### 固定层：新增设计必须遵守

- 蓝白双色系统及其反相状态。
- 直角、细边框、硬偏移阴影。
- Serif 展示标题与 Monospace 系统文本的字体分工。
- 以 4px / 8px 为基础的间距节奏和 32px 默认背景网格。
- 1-bit、线框化、可辨识的系统图标。
- 明确的 hover、pressed、selected、focused、running、minimized 状态。
- Windows / macOS 两种桌面布局共享同一组件语言，只改变系统 chrome 与应用入口位置。
- 英文、简体中文、日文三种语言下均不溢出、不遮挡、不改变信息层级。
- `prefers-reduced-motion` 下仍能看懂全部状态变化。

### 可演化层：允许应用按内容发展

- Projects、Writing、About、Contact 的内部信息架构与内容版式。
- 图片、视频、代码、文章排版等作品内容的呈现方式。
- BOT 的人格、对话结构与完整状态机。
- 新的系统级显示效果，但它们必须由蓝白两色构成，并具备明确的“设备输出”语义。

应用内容可以比系统壳层更丰富，但图片或媒体应作为“被系统承载的内容”，不能反过来改变窗口、导航、控制器和状态反馈的基础语言。

## 3. 颜色

### 基础色

| Token | 值 | 用途 |
| --- | --- | --- |
| `--blue` | `#183B9B` | 文字、边框、主状态、标题栏、硬阴影（色相收敛至环境蓝 224°） |
| `--white` | `#ffffff` | 页面底色、窗口底色、反相文字、图标填充 |

蓝白二色是品牌识别，不为新模块新增独立强调色。弱化层级时，只允许使用蓝色透明度或蓝白混合，例如背景网格使用约 6% 蓝，禁用态和次要信息通过透明度、线型或字号区分。

### 颜色使用规则

- 默认状态：白底蓝字、蓝色 1px 边框。
- 强调 / 选中 / 当前状态：蓝底白字。
- 聚焦：`2px solid var(--blue)`，通常配合 `2px` 外偏移。
- 危险操作不引入红色；通过清晰标签、位置、确认流程或更强的蓝白反相表达。
- 图像内容允许保留原色，但系统 chrome、按钮、标签和状态指示器仍保持蓝白。
- 禁止彩色渐变、霓虹光、米色纸张底、灰色拟物层、透明玻璃和背景模糊。
- CSS gradient 仅可用于网格、扫描线、dither、moire 等技术纹理，不得作为装饰性光影或品牌渐变。

## 4. 字体与文字层级

### 字体角色

```css
--serif: "Times New Roman", "Songti SC", "Yu Mincho", serif;
--mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
```

- `--serif`：站点标题、应用主标题、窗口内容中的章节标题。
- `--mono`：按钮、导航、状态、字段标签、协议、应用名、辅助说明和所有系统 chrome。
- 不额外引入无衬线品牌字体。正文若未来需要长篇阅读，可在 Writing 应用内定义专用阅读字体，但不能替代系统字体。

### 推荐字号

| 场景 | 字号 | 规则 |
| --- | --- | --- |
| 启动主标题 | `32-54px` | Serif，正常字重，紧凑行高 |
| 应用 / 面板主标题 | `25-32px` | Serif，正常字重 |
| 窗口标题 | `12px` | Mono，`700` |
| 系统正文 / 控件 | `10-12px` | Mono |
| 图标标签 | `11px` | Mono，允许自然换行 |
| 协议码 / 辅助状态 | `10px` | Mono |

### 排版规则

- 不使用负字距；CJK 文本必须保持 `letter-spacing: 0`。
- 英文系统标签可大写，但自然语言标题不要全部大写。
- 长标题优先截断并提供完整可访问名称；字段标签优先换行，不用缩到不可读。
- 数值、单位和协议写法保持紧凑，如 `32px`、`60Hz`、`TCP/IP / SECURE`。
- 文案应像状态输出或工具标签：短、具体、可执行，避免营销语、解释性口号和俏皮提示。

## 5. 空间、网格与构图

### 基础节奏

- 以 `4px` 为最小单位、`8px` 为主要间距单位。
- 常用间距：`4 / 6 / 8 / 12 / 16 / 24 / 28 / 32px`。
- 默认桌面网格为 `32px`；显示设置可在 `16-64px` 内调整，但新增布局不得依赖某一个网格密度才能成立。
- 窗口内容默认内边距为 `24px`，复杂面板可用 `28px`；窄屏降为 `16px`。

### 构图原则

- 桌面应保留明显的负空间，让窗口、图标和 BOT 像系统对象一样落在坐标场中。
- 窗口内部按任务组织，不使用营销页式 hero，也不把每个区块包成卡片。
- 使用边线、留白和反相标题区建立层级；避免卡片套卡片。
- 固定格式元素必须有稳定尺寸，状态和文字变化不能引发布局跳动。
- Windows 模式从左上组织桌面图标，底部任务栏承载运行态。
- macOS 模式顶部为菜单栏，底部居中 Dock；它不是独立视觉主题，不使用彩色 traffic-light 控件或磨砂 Dock。

## 6. 形状、边框与阴影

### 形状

- 所有系统组件使用 `border-radius: 0`。
- 基础描边为 `1px solid var(--blue)`。
- 需要章节强调时可使用 `4px double var(--blue)`。
- 次级分隔可用 `1px dotted var(--blue)`；最小化等非活跃状态可用 dashed border。
- 不使用药丸、圆形浮动按钮或大圆角容器。

### 硬阴影层级

| 偏移 | 用途 |
| --- | --- |
| `3-4px` | 小按钮、图标、轻量控件 |
| `6px` | 普通窗口、悬停后的图标 |
| `9px` | 当前活动窗口 |
| `12px` | 启动面板或唯一主舞台 |

阴影必须是无模糊、无扩散的纯蓝色块。按下状态可把偏移压缩到 `1px`，模拟明确的机械反馈。不要用软阴影制造悬浮卡片。

## 7. 图标与图形资产

- 系统图标以 `46 x 46px` 为标准画布，白底、1px 蓝框、`4px` 蓝色硬阴影。
- 图形使用 1px 线条、矩形、字符和少量实心块构成，保持 1-bit 观感。
- 新图标必须先保证对象轮廓可辨识，再考虑风格化；不能依赖颜色区分含义。
- 优先用 CSS、字符或像素位图实现系统图标，不使用现代圆角应用图标、emoji、彩色插画或未经处理的第三方 icon set。
- 选中时图标和标签同时反相：蓝底白色图形 / 文字。
- 照片、视频和项目截图属于内容资产，应显示真实作品，不做模糊氛围背景；必要时用蓝色边框或系统标签把它们纳入界面。

## 8. 核心组件规范

### 应用图标

- Windows 桌面单元宽 `88px`、最小高 `80px`，图标与标签间距 `8px`。
- 精细指针设备：单击选中、双击打开；触屏与窄屏：单击打开。
- hover 只做短距离上移与硬阴影增强；macOS Dock 可略强，但不做弹簧放大链。
- 必须支持键盘方向键导航、`Enter` 打开及清晰 focus ring。

### 窗口

- 白底、1px 蓝框、普通 `6px` 硬阴影；活动窗口提升为 `9px`。
- 标题栏高 `32px`，蓝底白字；控件至少 `32 x 32px`，白底蓝色符号。
- 控件使用熟悉的符号，如最小化 `-`、关闭 `X`，并提供本地化的可访问名称。
- 同一应用只保留一个窗口实例；再次启动应聚焦或恢复，而不是叠加副本。
- 窗口状态必须包含 active、inactive、minimized、restored、closed。
- 桌面可拖动；窄屏窗口固定铺满可用工作区，不显示无意义的拖动提示。

### 导航与设置控件

- 当前项用蓝底白字反相，不依赖下划线或颜色微差。
- Select、range、checkbox 使用原生语义并以蓝色 accent 统一。
- 表单行以 dotted divider 分组；标签与控件在宽屏并列、窄屏垂直堆叠。
- 二元状态使用 checkbox / toggle，数值使用 range，有限选项使用 select，不把所有控制都做成文字按钮。

### 系统栏、Dock 与状态

- 系统栏高 `32-48px`，只承载全局状态和高频入口。
- 状态文本保持单行、可扫描，以边线和空隙分组。
- 运行应用使用反相表示当前态，dashed border 表示最小化。
- BOT 等常驻服务应贴近边缘但避开系统栏和 Dock，不遮挡窗口控件或应用入口。

## 9. 动效与反馈

### 时间与曲线

```css
--press-duration: 140ms;
--ui-duration: 200ms;
--ease-out: cubic-bezier(0.23, 1, 0.32, 1);
--ease-in-out: cubic-bezier(0.77, 0, 0.175, 1);
```

- 常规 hover、选择、窗口显隐控制在 `120-200ms`。
- 按压先改变位移或阴影，再改变颜色；反馈必须短而清楚。
- 启动、扫描、数据包等“系统过程”使用 `steps()`，形成离散输出感。
- 只有表达状态变化时才运动。禁止持续漂浮、装饰性视差、弹簧动画和大幅缩放。
- 新动效必须回答：什么状态发生了变化、用户是否能中断、减少动态后如何表达同一信息。

### Reduced Motion

- 在 `prefers-reduced-motion: reduce` 下把过渡压缩到约 `1ms` 或移除。
- 停止循环信号动画、icon jitter 和分步移动。
- 不得移除最终状态、focus、选中、运行或错误反馈。

## 10. 响应式与多语言

### 断点原则

- `760px` 及以下进入窄屏系统模式。
- 窄屏窗口占据除系统 chrome 外的可用空间，窗口打开时隐藏被遮挡的桌面图标 / Dock。
- 设置侧栏在窄屏转为顶部四列导航，字段从多列转为单列。
- 图标触控目标保持约 `76px` 单元；窗口控制不能小于 `32px`。
- 不按视口宽度连续缩放字体，使用明确断点和稳定字号。

### 国际化

- 所有可见文案从 locale dictionary 获取，禁止组件内硬编码英文。
- 同时验证 `en`、`zh-CN`、`ja`，以最长文案决定控件宽度和换行策略。
- 协议型 token 可保持不翻译，例如 `[OK]`、`BUILD: 882.A`、`TCP/IP`。
- 切换语言不刷新页面，并同步更新已打开窗口、标题、辅助文本和 document title。

## 11. 可访问性

- 交互元素必须使用语义化的 `button`、`input`、`select`、`nav` 等元素。
- 所有 icon-only 控件必须有本地化 accessible name。
- 键盘可以完成图标选择、应用打开、窗口控制、设置修改和语言切换。
- focus ring 不得因视觉“更干净”而移除。
- 状态变化使用 `aria-pressed`、`aria-live` 或等价语义，不只依赖视觉反相。
- 文字与背景保持蓝白高对比；通过透明度弱化的内容仍需满足可读性。
- 组件必须在 200% 缩放、窄屏和三种语言下保持可操作，不允许文字与控件重叠。

## 12. 禁止模式

后续设计不得出现以下偏离：

- 新增第三种 UI 强调色，或为每个应用分配品牌色。
- 圆角卡片、药丸标签、玻璃拟态、背景模糊、软阴影、发光描边。
- 把桌面做成营销落地页，在首屏放 hero、功能卖点或解释性文案。
- 大量独立卡片或卡片嵌套，用容器代替真正的信息层级。
- 装饰性渐变、光斑、bokeh、漂浮几何体或无功能的 3D 元素。
- 直接复制 Windows / macOS 原生视觉资产，造成品牌拼贴。
- 使用 emoji 或彩色通用图标替代现有 1-bit 图标系统。
- 为“复古感”故意降低可用性，例如隐藏焦点、缩小触控目标、使用难读的像素字体。
- 不解释状态的动画、无止境的环境运动或与系统节奏不一致的弹簧效果。
- 在组件中硬编码语言、只验证英文、用缩小字号解决 CJK 溢出。

## 13. 新增设计元素的决策顺序

每次新增元素，依次回答：

1. 它是系统 chrome、交互控件、状态反馈，还是被系统承载的内容？
2. 它解决什么任务，是否已有组件或状态可以复用？
3. 默认、hover、pressed、selected、focus、disabled、loading、error 各是什么状态？
4. 它能否只用蓝、白、线型、反相和硬阴影表达层级？
5. 它在 Windows 与 macOS 桌面模式中是共享组件还是位置变化？
6. 它在 `390px` 窄屏、`1440px` 桌面、200% 缩放下是否稳定？
7. 英文、中文、日文最长文案是否能完整显示或合理截断？
8. 减少动态后，状态变化是否仍然明确？

如果前三个问题没有清晰答案，不应先创造视觉样式。

## 14. 交付检查清单

- [ ] 仅使用既有蓝白 token，或有充分理由把原色媒体作为内容展示。
- [ ] 所有系统容器保持直角、1px 蓝框和规定的硬阴影层级。
- [ ] Serif / Mono 的角色没有混用。
- [ ] 复用了现有间距、标题栏、控件和状态模式。
- [ ] 没有新增装饰性卡片、渐变、软阴影、模糊或第三强调色。
- [ ] hover、pressed、selected、focus、disabled 和异步状态完整。
- [ ] Windows / macOS 两种模式下位置正确且行为一致。
- [ ] `390 x 844` 与 `1440 x 900` 下没有遮挡、溢出或布局跳动。
- [ ] `en`、`zh-CN`、`ja` 均已检查。
- [ ] 键盘、触屏、精细指针与 reduced motion 均可用。
- [ ] 新增文案已进入 i18n dictionary，图标有 accessible name。

## 15. 当前实现的参考入口

- 视觉 token：[`styles/tokens.css`](./styles/tokens.css)
- 桌面网格与显示效果：[`styles/shell.css`](./styles/shell.css)
- 图标系统：[`styles/icons.css`](./styles/icons.css)
- Windows / macOS chrome：[`styles/windows-mode.css`](./styles/windows-mode.css)、[`styles/macos-mode.css`](./styles/macos-mode.css)
- 窗口语言：[`styles/windows.css`](./styles/windows.css)
- 应用内部控件：[`styles/apps.css`](./styles/apps.css)
- 窄屏规则：[`styles/responsive.css`](./styles/responsive.css)
- 启动体验：[`styles/boot.css`](./styles/boot.css)
- 产品与交互背景：[`docs/superpowers/specs/2026-08-11-portfolio-os-desktop-shell-design.md`](./docs/superpowers/specs/2026-08-11-portfolio-os-desktop-shell-design.md)

当文档与实现发生差异时：先判断是实现偏离了固定层，还是项目确实需要演化固定层。前者应修正实现；后者必须同步更新本文档与相关 token，不能只在局部组件中留下例外。
